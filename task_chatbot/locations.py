from os import path

ROOT_DIR = path.dirname(path.abspath(__file__))
RESOURCES_PATH = path.join(ROOT_DIR, 'resources')
GUI_IMG_PATH = path.join(ROOT_DIR, 'gui', 'img')
MODEL_FILE_PATH = path.join(RESOURCES_PATH, "agent_models")
